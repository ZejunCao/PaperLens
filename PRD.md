# PaperLens 产品需求文档

> PaperLens 是一款本地单用户 AI 论文阅读器，提供句子级对照翻译、引用快捷跳转和交互式阅读辅助。

## 1. 产品概述

- **目标用户**：需要阅读英文论文的学生、工程师和研究人员
- **产品形态**：本地运行、桌面端优先的单用户 Web 应用
- **长期愿景**：从论文阅读器逐步发展为个人 AI Paper IDE
- **MVP 闭环**：

```text
上传原生 PDF → 解析段落与句子 → 句子级对照阅读 → 引用跳转与 AI 辅助理解
```

### 1.1 用户痛点

1. 双栏排版、公式和图表降低阅读效率。
2. 阅读、翻译、检索和 AI 对话分散在多个工具中。
3. 全文翻译成本高，且译文难与原文准确对应。
4. 遇到 `[13]` 等正文引用时，需要翻到参考文献确认论文，再返回原位置。

### 1.2 MVP 目标

- 在同一界面完成上传、阅读、按需翻译和 AI 辅助理解。
- 将 PDF 解析为章节、段落和句子，同时保留页码与位置。
- 翻译按可见区域或段落懒加载，避免无效模型调用。
- 原文与译文按句子对应，悬浮原文句子时突出显示对应译文。
- 悬浮正文引用时展示对应论文及链接，点击后直接打开。
- 仅支持包含可提取文本的原生 PDF。

### 1.3 MVP 非目标

- 扫描件与纯图片 PDF
- 图表理解、公式推导和实验复现
- 引用图谱、跨论文知识库和外部论文推荐
- 多人协作、论文写作和投稿管理
- 多模型平台、本地大模型部署和移动端深度适配

## 2. 核心用户故事

1. 用户可以上传论文，并看到解析进度、失败原因和重试入口。
2. 用户可以按段落阅读原文和译文，并看到句子级对应关系。
3. 用户可以只翻译当前段落或可见区域。
4. 用户可以选择文本并执行翻译、解释或背景补充。
5. 用户可以让 AI 解答当前不理解的内容，或生成论文简略概要。
6. 用户悬浮正文引用时可以预览对应论文，点击链接可直接打开。
7. 用户可以删除论文及其文件、译文、索引和对话记录。

## 3. MVP 功能需求

优先级：P0 为 MVP 必须完成，P1 为主流程稳定后补充。

### 3.1 论文库与 PDF 阅读（P0）

- 上传、打开、重命名和删除 PDF。
- 展示文件名、标题、页数、上传时间和处理状态。
- 校验文件类型、文件头和重复文件。
- 使用 PDF.js 支持缩放、翻页和页码跳转。
- 上传完成后可立即阅读原始 PDF，无需等待解析。
- 检测到扫描件或纯图片 PDF 时提示暂不支持。

处理状态至少包括：

```text
uploaded → queued → parsing → ready / failed
```

验收标准：

- 上传成功后返回论文 ID，并异步开始解析。
- 刷新页面后论文和任务状态不丢失。
- 失败时展示原因并允许重试。
- 删除论文后不残留任何衍生数据。

### 3.2 PDF 内容解析（P0）

- 提取章节、段落、句子、公式、图片、图注、表格和参考文献。
- “结构化”仅表示将 PDF 内容还原为有顺序的章节、段落和句子，并保留其页码与坐标，不改变论文内容。
- 每个段落和句子保存稳定 ID、顺序、页码和坐标。
- 建立标准 Document JSON，统一解析输出格式。
- 首版采用 PyMuPDF 解析（版式坐标 + 图表区域裁剪保存）。
- 整篇论文异步解析；懒加载仅用于翻译和 AI 功能。
- 无法可靠解析时允许降级为纯 PDF 阅读。

内容块示例：

```json
{
  "id": "block_001",
  "type": "paragraph",
  "page": 3,
  "order": 12,
  "bbox": [72, 120, 520, 260],
  "source_text": "The model uses attention. It improves accuracy.",
  "sentences": [
    {"id": "sentence_001", "text": "The model uses attention."},
    {"id": "sentence_002", "text": "It improves accuracy."}
  ]
}
```

验收标准：

- 固定测试集中的主要正文能按正确阅读顺序提取。
- 点击结构化内容可定位到 PDF 对应页面。
- 重复执行解析不会生成重复数据。

### 3.3 句子级对照阅读与按需翻译（P0）

- 提供原文、译文和对照三种阅读模式。
- 对照模式左侧保留原始 PDF，右侧按原文段落展示中文译文。
- 每个 PDF 原文句子和译文句子通过稳定的 `sentence_id` 一一对应。
- 鼠标悬浮 PDF 文本层中的英文句子时，右侧对应中文句子突出显示；移开后恢复。
- 可选支持反向悬浮，即悬浮译文时突出对应原文。
- 支持翻译当前段落和当前可见区域；章节翻译放入 P1。
- 公式主体不翻译，专有名词在同一论文内尽量保持一致。
- 相同原文、目标语言、模型和提示词版本使用缓存。
- 单个内容块翻译失败时不阻断整篇论文阅读。

验收标准：

- 任一译文句子都能定位到 PDF 文本层中的唯一原文句子。
- 悬浮任一原文句子时，只突出对应译文句子。
- 切换阅读模式不丢失当前位置。
- 未进入可见区域的内容不会自动翻译。
- 已完成译文再次打开时直接读取缓存。

### 3.4 引用悬浮预览与跳转（P0）

- 识别正文中的 `[13]`、`[2, 5]`、`[7–9]` 等引用标记。
- 将引用标记关联到文末对应参考文献条目。
- 鼠标悬浮引用时，弹窗展示论文标题、作者、年份和可用链接。
- 点击标题或链接后，在新标签页打开 DOI、arXiv 或论文页面。
- 如果参考文献本身没有链接，可通过 DOI 或标题查询公开元数据。
- 匹配置信度不足时只展示原始参考文献，不提供可能错误的外部链接。

验收标准：

- 悬浮引用标记时，无需离开当前位置即可查看对应论文信息。
- 点击有效链接能够打开对应论文页面。
- 多篇引用和连续编号可以分别查看。
- 外部链接查询失败不影响基础阅读。

### 3.5 AI 阅读助手（P0 基础版，后续持续扩展）

- 用户预先填写自己的模型 API Key，PaperLens 不提供内置模型额度。
- 支持围绕选中句子、段落或当前章节进行交互式提问。
- 支持解释术语、通俗改写、背景知识补充和简略概要。
- 支持生成论文整体概要，以及章节的研究问题、方法、结论和局限。
- 保存论文级会话，支持新建和删除。
- AI 能力采用可扩展工具入口，后续逐步增加图表理解、公式解释和论文对比等功能。
- AI 输出用于辅助理解，不要求每条回答生成论文式引用。

验收标准：

- 选中文本后可直接发起解释，并自动携带当前上下文。
- 可以生成论文或章节的简略概要。
- 模型调用失败后可重试，且不产生重复消息。
- 删除论文时同步删除相关 AI 会话。

## 4. 核心交互流程

1. 用户上传 PDF，系统创建论文记录和解析任务。
2. 用户可立即阅读原始 PDF，同时查看解析进度。
3. 解析完成后，系统展示目录和结构化对照阅读界面。
4. 用户主动请求或滚动到内容时触发按需翻译。
5. 用户悬浮正文引用时预览对应论文，点击链接直接打开。
6. 用户选择不理解的内容进行提问，或生成论文简略概要。
7. 解析或模型调用失败时，保留基础阅读能力并允许重试。

阅读器布局：

- 左侧：目录与页面导航
- 中间：原始 PDF 及可交互文本层
- 右侧：按段落展示的译文、解释或 AI 问答
- 顶部：阅读模式、翻译范围和模型设置

## 5. 数据与技术约束

### 5.1 核心数据

- `Paper`：论文元数据、文件位置和处理状态
- `Section` / `Block`：章节和结构化内容
- `Translation`：块级译文及模型、提示词版本
- `Conversation` / `Message`：AI 阅读助手的会话记录
- `Reference` / `CitationLink`：参考文献及正文映射
- `Job`：解析、翻译和索引等异步任务

所有衍生数据必须关联 `paper_id`，以支持完整删除；原始解析结果与标准化 Document JSON 分开保存。

### 5.2 技术栈

- 前端：Vue 3、TypeScript、Vite、Pinia、Element Plus、PDF.js
- 后端：FastAPI、Pydantic、SQLAlchemy、Alembic
- 数据库：MVP 使用 SQLite
- 文件：MVP 使用本地文件系统
- 解析：PyMuPDF，输出统一 Document JSON
- 模型：用户自带 API Key，MVP 先实现一个 OpenAI-compatible Provider

### 5.3 后端约束

- 解析和模型调用不得阻塞 API 请求线程。
- 耗时请求返回 `202 Accepted` 和 `job_id`。
- 任务状态必须持久化，并支持幂等、失败重试和取消。
- MVP 可使用数据库任务表和独立 Worker；并发增长后再引入 Redis 与专业任务队列。
- AI 助手优先使用用户选中的文本和当前章节作为上下文；需要全文能力时再增加检索。

## 6. 非功能需求

### 6.1 安全与隐私

- 产品层面暂不规定文件大小、页数和并发上限；实现时保留可配置的资源保护参数。
- 文件名不得直接作为存储路径，防止路径穿越。
- API Key 由用户在首次使用前填写，仅存储在本机安全配置中，不上传到 PaperLens 服务，并在日志中脱敏。
- 日志不记录论文全文、完整提示词或模型密钥。
- 用户可以删除原文件和全部衍生数据。
- 云服务版本需补充账号隔离、隐私政策和数据保留策略。

### 6.2 可观测性

- 记录任务耗时、失败阶段、重试次数和模型用量。
- 为 API 请求和异步任务生成可关联的追踪 ID。
- 管理信息不得暴露敏感论文内容。

## 7. MVP 质量指标

- 固定测试集至少包含 20 篇单栏、双栏、含公式和含表格的原生文本型论文。
- PDF 上传后 3 秒内可查看首页。
- 常见文本型论文解析成功率不低于 95%。
- 成功解析内容的主要阅读顺序正确率不低于 90%。
- 翻译缓存命中时，译文展示延迟不超过 500 ms。
- 句子级原文与译文对应准确率不低于 95%。
- 测试集中可识别的正文引用均能展示对应参考文献，存在公开链接时可正常打开。
- 主流程不存在阻断级错误。

具体解析耗时目标应在参考部署环境完成 PyMuPDF 基准测试后确定，不提前承诺固定数值。

## 8. 迭代路线

### Milestone 0：工程基础

- 前后端初始化、数据库迁移
- PDF 上传、论文库和 PDF.js 阅读

### Milestone 1：内容解析

- PyMuPDF 解析器和异步任务
- Document JSON、目录、段落、句子与 PDF 定位
- 失败提示和重试机制

### Milestone 2：MVP 闭环

- 句子级对照阅读与悬浮联动
- 懒加载翻译与缓存
- 引用悬浮预览、外部链接跳转
- AI 解释与简略概要
- 数据删除、安全校验和自动化测试

### Milestone 3：体验增强

- 更多 AI 阅读工具和章节总结
- 图表和公式辅助解释

### 长期探索

- 外部论文元数据补全
- 跨论文检索、对比和引用图谱
- 个人研究知识库

## 9. 风险与应对

- **复杂版式解析错误**：保留原始 PDF 模式；必要时再评估更重的版面分析方案。
- **内容块与 PDF 坐标错位**：保存页码、坐标和文本特征，使用固定测试集回归。
- **图表被拆成散字**：对矢量绘制与图片区域聚类裁剪为位图，图内文字不再单独抽取。
- **模型成本不可控**：按需调用、缓存结果、设置额度并允许取消任务。
- **AI 回答不准确**：明确辅助定位，优先携带选中内容和当前章节上下文。
- **任务中断或重复执行**：持久化状态并保证处理逻辑幂等。

## 10. MVP 完成定义

1. 新用户无需手工操作后端即可完成上传、阅读、翻译和问答。
2. 固定测试集通过完整主流程测试，并达到质量指标。
3. 所有异步任务均有状态、失败原因和重试机制。
4. 原文和译文可按句子悬浮联动。
5. 正文引用可悬浮查看论文信息，并通过有效链接跳转。
6. 删除论文后不残留文件、解析结果、译文、索引或对话。
7. 核心 API 有自动化测试，前端至少有一条主流程端到端测试。

## 11. 已确认产品决策

1. 首版为本地单用户应用，不开发账号体系。
2. 用户需要提前填写自己的模型 API Key。
3. 产品暂不设固定文件大小、页数和并发上限，后续根据实际性能确定。
4. 不支持扫描件，仅支持包含可提取文本的原生 PDF。
5. 对照阅读按段落排版、按句子对齐；悬浮英文句子时突出显示对应中文句子。
